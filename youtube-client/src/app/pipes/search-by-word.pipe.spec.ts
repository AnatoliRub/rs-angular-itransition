import { SearchByWordPipe } from './search-by-word.pipe';

describe('SearchByWordPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByWordPipe();

    expect(pipe).toBeTruthy();
  });
});
